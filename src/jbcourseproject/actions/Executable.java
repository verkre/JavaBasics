package jbcourseproject.actions;

/**
 * This interface is here just to test the use of interfaces - the execute() method 
 * could be an abstract method in the Action superclass since that's the only class that 
 * implements it (right now).
 */
public interface Executable {
    void execute();
    
}
